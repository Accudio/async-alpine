const config = {
  prefix: 'ax-',
  root: 'ax-load',
  src: 'ax-load-src',
  defaultStrategy: 'immediate',
  alpine: {
    prefix: 'x-',
    data: 'x-data',
    cloak: 'x-cloak',
  },
};

export default config;
