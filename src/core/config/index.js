const config = {
  prefix: 'ax-',
  root: 'ax-load',
  src: 'ax-load-src',
  id: 'ax-id',
  defaultStrategy: 'immediate',
  alpine: {
    prefix: 'x-',
    attributes: [ 'x-', ':', '@' ]
  },
};

export default config;
