const config = {
  prefix: 'ax-',
  root: 'load',
  src: 'load-src',
  id: 'id',
  defaultStrategy: 'immediate',
  alpine: {
    prefix: 'x-',
    attributes: [ 'x-', ':', '@' ],
  },
};

export default config;
